# Design QA — hero «Аудиторская линза»

## Источники и состояние

- Визуальный эталон: артефакт генерации `019fada5-592e-7310-994e-eac3a5a2f607/call_HULRyYp4gIw0Qh8vcg4xwYHo.png` (в репозиторий не включён).
- Реализация, desktop: `design_refs/07-audit-lens-desktop.png`.
- Реализация, mobile: `design_refs/08-audit-lens-mobile.png`.
- Полное сравнение в одном изображении: `design_refs/09-audit-lens-qa-comparison.png`.
- Фокусное сравнение hero-объекта: `design_refs/10-audit-lens-qa-focus.png`.
- Состояние: главная страница, верх страницы, тёмная тема, анимация появления завершена.
- CSS viewport desktop: `1440 × 1024`, device scale factor `1`.
- Исходный макет: `1505 × 1045 px`.
- Desktop-снимок реализации: `1425 × 1013 px`; браузер исключил вертикальную полосу прокрутки и служебную область из итогового bitmap.
- CSS viewport mobile: `390 × 844`; mobile-снимок реализации: `375 × 811 px` по той же причине.
- Нормализация: для полного сравнения оба изображения помещены в равные области `1440 × 1024` без растяжения; для фокусного сравнения сопоставлены одинаковые смысловые области hero.

## Проверка обязательных поверхностей

- Шрифты и типографика: существующая утверждённая пара serif/sans, размеры, переносы, насыщенность и иерархия сохранены. На desktop и mobile нет обрезанного текста.
- Ритм и компоновка: структура первого экрана, отступы, кнопки и переход к светлому разделу не изменены. Горизонтального переполнения нет.
- Цвета и токены: сохранены `#0B0B0B`, тёплый кремовый и приглушённая латунь. Красный используется точечно для расхождения и proof-line.
- Качество изображения: отдельный hero-ассет `1536 × 1024`, WebP около 90 КБ, визуально резкий; объект не заменён CSS-графикой или заглушкой. На desktop видны регистр, линза, трассировка и красное исключение; на mobile сохранён смысловой фокус и корректный кроп.
- Текст и содержание: весь пользовательский текст, навигация и CTA сохранены без изменений.

## Полное и фокусное сравнение

- Полный экран: композиция, тёмный фон, масштаб заголовка, положение CTA и начало следующего раздела соответствуют действующей дизайн-системе и выбранному макету.
- Hero-фокус: форма регистра, латунная линза, строки Дт/Кт, трассировка и одиночное красное расхождение воспроизведены напрямую в растровом ассете.
- Неблокирующее отличие P3: объект в реализации немного компактнее, чем в генеративном макете. Это сохраняет безопасную дистанцию от заголовка и предотвращает обрезание на промежуточных desktop-ширинах; корректировка не требуется.

## Проверенные взаимодействия

- Мобильное меню открывается, имеет `aria-expanded="true"` и закрывается клавишей Escape.
- Основная CTA «Начать с первого материала» прокручивает к `#materials`.
- Ошибок в консоли браузера не обнаружено.
- Автоматические Flask-тесты: `3` теста, все пройдены.

## История итераций P0/P1/P2

- Первая браузерная проверка не выявила P0, P1 или P2 расхождений.
- Исправления уровня P0/P1/P2 не потребовались; повторный desktop- и mobile-снимок подтвердил отсутствие переполнения, корректный кроп и загрузку нового ассета.

## Follow-up polish

- P3 по желанию: увеличить объект на 3–5% на очень широких экранах, если потребуется более драматичная подача.

final result: passed

---

# Design QA — страница «Обо мне»

## Источники и состояние

- Визуальный источник: `cc8ca1ab2a615e9295aeeb00348d299e_67b1f8ab-9352-402d-8bdf-35af9ead2a73.png` — портрет с прямым взглядом, блокнотом и ручкой, без ретуши и изменения внешности.
- Источник дизайн-системы: утверждённая главная страница и её снимок `design_refs/07-audit-lens-desktop.png`.
- Реализация первого экрана: `design_refs/12-about-viewport.jpg`.
- Последовательная проверка разделов: `design_refs/about-section-1.jpg` — `design_refs/about-section-8.jpg`.
- Реализация нового фото-блока: `design_refs/18-about-open-photo-final.jpg`.
- Реализация обновлённой подписи: `design_refs/19-about-caption-final.jpg`.
- Нормализованное сравнение исходного фото, кропа и подписи в одном изображении: `design_refs/21-about-open-photo-qa.jpg`.
- Проверенное состояние: страница `/about`, тёмная тема, анимация появления завершена.

## Проверка обязательных поверхностей

- Типографика и токены продолжают существующую serif/sans-систему сайта; латунный акцент, кремовые поверхности и красный предупреждающий маркер используются последовательно.
- Фото оптимизировано до `1080 × 1935`, около 215 КБ. Лицо, одежда и фон не изменялись; применены только адаптивный кроп и лёгкая тональная коррекция под тёмный фон.
- Строка «профессионального опыта» размещена непосредственно под «20 лет» и образует единую смысловую группу.
- Формулировка «Практик, не наблюдатель» выровнена справа, отделена тонкой латунной линией и визуально сбалансирована с блоком стажа.
- На ширине встроенного браузера `754 px` горизонтального переполнения нет: ширина содержимого и ширина прокрутки совпадают.
- Все восемь контрольных состояний просмотрены отдельно: герой и фото, причина проекта, метод, четыре шага, принципы, практические задачи, финальная CTA и подвал.
- Текст опирается только на подтверждённые факты: 20 лет опыта, действующая роль заместителя главного бухгалтера и практический подход к ИИ.

## Проверенные взаимодействия

- Ссылки «Посмотреть материалы» и «Описать задачу» ведут на `/#materials` и `/#contact`.
- Пункт «Обо мне» присутствует в основной навигации и подвале.
- В материалах добавлена ссылка на автора с подтверждённым стажем.
- Ошибок в журнале браузера нет.
- Автоматические Flask-тесты: `5` тестов, все пройдены.

## История итераций

- P1: браузер сохранил старую версию CSS из-за длительного кеширования. Добавлена версия статических ресурсов; повторная загрузка подтвердила корректное оформление.
- P2: одновременно работали несколько старых локальных процессов Flask. Они были остановлены, после чего сайт запущен одним актуальным процессом.
- P2: исходное фото со скрещёнными руками заменено портретом с прямым взглядом и рабочими предметами. Новый вариант лучше поддерживает открытый контакт и сохраняет профессиональный контекст.
- P2: прежняя сетка подписи разрывала фразу «20 лет профессионального опыта». Блок перестроен: пояснение находится под числом, а позиционирование «Практик, не наблюдатель» стало симметричным.
- Повторная проверка подтвердила загрузку фото, отсутствие горизонтального переполнения, работу навигации и корректное отображение всех разделов.

## Follow-up polish

- После подтверждения содержания можно добавить имя и специализацию в первый экран, если пользователь захочет сделать эти данные публичными.

final result: passed
